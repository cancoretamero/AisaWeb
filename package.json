{
  "name": "aisaweb",
  "private": true,
  "version": "0.1.0",
  "description": "Aisa Group website + Liquid Command Center console",
  "scripts": {
    "build": "echo \"AisaWeb: no build step (static site)\" && exit 0"
  },
  "engines": {
    "node": ">=18"
  }
}
